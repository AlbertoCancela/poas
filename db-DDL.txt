-- POAS_CUENTAS definition

-- Drop table

-- DROP TABLE POAS_CUENTAS;

CREATE TABLE POAS_CUENTAS (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255),
	DESCRIPCION VARCHAR(4000),
	CONSTRAINT INTEG_934 PRIMARY KEY (ID)
);
CREATE UNIQUE INDEX RDB$PRIMARY40 ON POAS_CUENTAS (ID);


-- POAS_DIRECCIONES definition

-- Drop table

-- DROP TABLE POAS_DIRECCIONES;

CREATE TABLE POAS_DIRECCIONES (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255) NOT NULL,
	CLAVE VARCHAR(255) NOT NULL,
	CONSTRAINT INTEG_902 PRIMARY KEY (ID)
);
CREATE UNIQUE INDEX RDB$PRIMARY32 ON POAS_DIRECCIONES (ID);


-- POAS_EJERECTOR definition

-- Drop table

-- DROP TABLE POAS_EJERECTOR;

CREATE TABLE POAS_EJERECTOR (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255),
	DESCRIPCION VARCHAR(2000),
	CLAVE VARCHAR(255),
	CONSTRAINT INTEG_957 PRIMARY KEY (ID)
);
CREATE UNIQUE INDEX RDB$PRIMARY43 ON POAS_EJERECTOR (ID);


-- POAS_NIVELES definition

-- Drop table

-- DROP TABLE POAS_NIVELES;

CREATE TABLE POAS_NIVELES (
	ID INTEGER NOT NULL,
	NIVEL VARCHAR(255),
	CONSTRAINT INTEG_920 PRIMARY KEY (ID)
);
CREATE UNIQUE INDEX RDB$PRIMARY36 ON POAS_NIVELES (ID);


-- POAS_UNIDADES definition

-- Drop table

-- DROP TABLE POAS_UNIDADES;

CREATE TABLE POAS_UNIDADES (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255),
	DESCRIPCION VARCHAR(4000),
	CONSTRAINT INTEG_932 PRIMARY KEY (ID)
);
CREATE UNIQUE INDEX RDB$PRIMARY39 ON POAS_UNIDADES (ID);


-- POAS_DEPARTAMENTOS definition

-- Drop table

-- DROP TABLE POAS_DEPARTAMENTOS;

CREATE TABLE POAS_DEPARTAMENTOS (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255) NOT NULL,
	ID_DIRECCION INTEGER NOT NULL,
	CLAVE VARCHAR(255) NOT NULL,
	CONSTRAINT INTEG_910 PRIMARY KEY (ID),
	CONSTRAINT FK_DEPARTAMENTOS_DIRECCIONES FOREIGN KEY (ID_DIRECCION) REFERENCES POAS_DIRECCIONES(ID)
);
CREATE INDEX FK_DEPARTAMENTOS_DIRECCIONES ON POAS_DEPARTAMENTOS (ID_DIRECCION);
CREATE UNIQUE INDEX RDB$PRIMARY34 ON POAS_DEPARTAMENTOS (ID);


-- POAS_LINEASACCION definition

-- Drop table

-- DROP TABLE POAS_LINEASACCION;

CREATE TABLE POAS_LINEASACCION (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255),
	DESCRIPCION VARCHAR(2000),
	CLAVE VARCHAR(255),
	ID_EJE_RECTOR INTEGER NOT NULL,
	CONSTRAINT INTEG_959 PRIMARY KEY (ID),
	CONSTRAINT FK_LINEASACCION_EJERECTOR FOREIGN KEY (ID_EJE_RECTOR) REFERENCES POAS_EJERECTOR(ID)
);
CREATE INDEX FK_LINEASACCION_EJERECTOR ON POAS_LINEASACCION (ID_EJE_RECTOR);
CREATE UNIQUE INDEX RDB$PRIMARY44 ON POAS_LINEASACCION (ID);


-- POAS_PROYECTOMETA definition

-- Drop table

-- DROP TABLE POAS_PROYECTOMETA;

CREATE TABLE POAS_PROYECTOMETA (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255),
	DESCRIPCION VARCHAR(2000),
	CLAVE VARCHAR(255),
	ID_LINEAS_ACCION INTEGER NOT NULL,
	CONSTRAINT INTEG_962 PRIMARY KEY (ID),
	CONSTRAINT FK_PROYECTOMETA_LINEASACCION FOREIGN KEY (ID_LINEAS_ACCION) REFERENCES POAS_LINEASACCION(ID)
);
CREATE INDEX FK_PROYECTOMETA_LINEASACCION ON POAS_PROYECTOMETA (ID_LINEAS_ACCION);
CREATE UNIQUE INDEX RDB$PRIMARY45 ON POAS_PROYECTOMETA (ID);


-- POAS_SECCIONES definition

-- Drop table

-- DROP TABLE POAS_SECCIONES;

CREATE TABLE POAS_SECCIONES (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255) NOT NULL,
	ID_DEPARTAMENTO INTEGER NOT NULL,
	CLAVE VARCHAR(255) NOT NULL,
	CONSTRAINT INTEG_915 PRIMARY KEY (ID),
	CONSTRAINT DK_SECCIONES_DEPARTAMENTOS FOREIGN KEY (ID_DEPARTAMENTO) REFERENCES POAS_DEPARTAMENTOS(ID)
);
CREATE INDEX DK_SECCIONES_DEPARTAMENTOS ON POAS_SECCIONES (ID_DEPARTAMENTO);
CREATE UNIQUE INDEX RDB$PRIMARY35 ON POAS_SECCIONES (ID);


-- POAS_USUARIOS definition

-- Drop table

-- DROP TABLE POAS_USUARIOS;

CREATE TABLE POAS_USUARIOS (
	ID INTEGER NOT NULL,
	NOMBRE VARCHAR(255) NOT NULL,
	USUARIO VARCHAR(255) NOT NULL,
	ID_NIVEL INTEGER NOT NULL,
	PERMISOS_EXTRA VARCHAR(8191),
	CORREO VARCHAR(255) NOT NULL,
	CONSTRAINT INTEG_922 PRIMARY KEY (ID),
	CONSTRAINT FK_USUARIOS_NIVELES FOREIGN KEY (ID_NIVEL) REFERENCES POAS_NIVELES(ID)
);
CREATE INDEX FK_USUARIOS_NIVELES ON POAS_USUARIOS (ID_NIVEL);
CREATE UNIQUE INDEX RDB$PRIMARY37 ON POAS_USUARIOS (ID);


-- POAS_POAS definition

-- Drop table

-- DROP TABLE POAS_POAS;

CREATE TABLE POAS_POAS (
	FOLIO INTEGER NOT NULL,
	FECHA_ELABORACION DATE NOT NULL,
	EJERCICIO_FISCAL VARCHAR(255) NOT NULL,
	DESCRIPCION VARCHAR(8191) NOT NULL,
	FECHA_INICIO DATE NOT NULL,
	FECHA_TERMINO DATE NOT NULL,
	ID_AREA INTEGER NOT NULL,
	TIPO_GASTO VARCHAR(255) NOT NULL,
	ID_PDI_PROYECTOMETA INTEGER,
	OBSERVACIONES VARCHAR(8191),
	AUTOR INTEGER NOT NULL,
	NOTA VARCHAR(8191),
	CONSTRAINT INTEG_946 PRIMARY KEY (FOLIO),
	CONSTRAINT FK_POAS_PROYECTOMETA FOREIGN KEY (ID_PDI_PROYECTOMETA) REFERENCES POAS_PROYECTOMETA(ID),
	CONSTRAINT FK_POAS_SECCIONES FOREIGN KEY (ID_AREA) REFERENCES POAS_SECCIONES(ID)
);
CREATE INDEX FK_POAS_PROYECTOMETA ON POAS_POAS (ID_PDI_PROYECTOMETA);
CREATE INDEX FK_POAS_SECCIONES ON POAS_POAS (ID_AREA);
CREATE UNIQUE INDEX RDB$PRIMARY42 ON POAS_POAS (FOLIO);


-- POAS_CONCEPTOACTIVIDAD definition

-- Drop table

-- DROP TABLE POAS_CONCEPTOACTIVIDAD;

CREATE TABLE POAS_CONCEPTOACTIVIDAD (
	ID INTEGER NOT NULL,
	FOLIO INTEGER NOT NULL,
	TIPO_CUENTA INTEGER NOT NULL,
	FECHA_EJECUCION VARCHAR(255) NOT NULL,
	CONCEPTO VARCHAR(4000) NOT NULL,
	UNIDAD INTEGER NOT NULL,
	CANTIDAD INTEGER NOT NULL,
	COSTO_UNITARIO INTEGER NOT NULL,
	IMPORTE_TOTAL INTEGER NOT NULL,
	OBSERVACIONES VARCHAR(4000),
	CONSTRAINT INTEG_936 PRIMARY KEY (ID),
	CONSTRAINT FK_CONCEPTOACTIVIDAD_CUENTAS FOREIGN KEY (TIPO_CUENTA) REFERENCES POAS_CUENTAS(ID),
	CONSTRAINT FK_CONCEPTOACTIVIDAD_POAS FOREIGN KEY (FOLIO) REFERENCES POAS_POAS(FOLIO),
	CONSTRAINT FK_CONCEPTOACTIVIDAD_UNIDADES FOREIGN KEY (UNIDAD) REFERENCES POAS_UNIDADES(ID)
);
CREATE INDEX FK_CONCEPTOACTIVIDAD_CUENTAS ON POAS_CONCEPTOACTIVIDAD (TIPO_CUENTA);
CREATE INDEX FK_CONCEPTOACTIVIDAD_POAS ON POAS_CONCEPTOACTIVIDAD (FOLIO);
CREATE INDEX FK_CONCEPTOACTIVIDAD_UNIDADES ON POAS_CONCEPTOACTIVIDAD (UNIDAD);
CREATE UNIQUE INDEX RDB$PRIMARY41 ON POAS_CONCEPTOACTIVIDAD (ID);


-- POAS_RESPONSABLES definition

-- Drop table

-- DROP TABLE POAS_RESPONSABLES;

CREATE TABLE POAS_RESPONSABLES (
	ID INTEGER NOT NULL,
	ID_CONCEPTO INTEGER NOT NULL,
	ID_USUARIO INTEGER NOT NULL,
	CONSTRAINT INTEG_928 PRIMARY KEY (ID),
	CONSTRAINT FK_RESPONSABLES_CONCEPTOACT FOREIGN KEY (ID_CONCEPTO) REFERENCES POAS_CONCEPTOACTIVIDAD(ID),
	CONSTRAINT FK_RESPONSABLES_USUARIOS FOREIGN KEY (ID_USUARIO) REFERENCES POAS_USUARIOS(ID)
);
CREATE INDEX FK_RESPONSABLES_CONCEPTOACT ON POAS_RESPONSABLES (ID_CONCEPTO);
CREATE INDEX FK_RESPONSABLES_USUARIOS ON POAS_RESPONSABLES (ID_USUARIO);
CREATE UNIQUE INDEX RDB$PRIMARY38 ON POAS_RESPONSABLES (ID);
